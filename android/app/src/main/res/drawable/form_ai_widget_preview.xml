<?xml version="1.0" encoding="utf-8"?>
<!-- Phase 55 · listed in res/xml/form_ai_widget_info.xml as the
     `previewLayout` reference fallback. We deliberately reuse the
     real layout's background so the preview in the widget picker
     matches what users get after pinning. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:startColor="#1A0D33"
        android:endColor="#0E0A1A"
        android:angle="135"/>
    <corners android:radius="20dp"/>
</shape>
