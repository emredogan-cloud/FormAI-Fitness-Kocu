// Placeholder smoke test. Full-app widget tests require stubbing
// dotenv + Supabase + camera, which we haven't set up yet.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
