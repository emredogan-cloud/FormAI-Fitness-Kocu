# Stash Triage Report — `closure-work-2026-05-22`

**Mode:** read-only inspection. Stash NOT popped, NOT applied. Working
tree and branch state unchanged.

**Source:** `stash@{0}: On feature/cdn-meal-migration: closure-work-2026-05-22`

**Size:** 31 files changed (+2,697 / −25,902 lines). Roughly 25,000 of
the deletions are `logs.txt` truncation; the substantive code delta is
about ±5,000 lines.

---

## 1 · Executive verdict

The stash is **dominated by a coordinated Progress Redesign initiative
(Phases 1.D / 1.E / 2.E / 2.F / 5.C) plus an unrelated Phase 100
home-workout filter**. It is **NOT directly apply-able** — 11 of its
consumer-side imports point at domain/widget files that exist in
*neither* HEAD *nor* the stash itself. Applying it as-is would leave
the build red. It is, however, **engineering work of real value** that
should be carried forward in a structured way, not dropped.

Two changes are already obsolete:
* `workout_plan_model.dart` (Phase 98 `premiumExercises`) — landed in
  HEAD as part of `7a742da fix(ci): restore analyze…`. The stash blob
  hash is `4aa5ed0`, identical to current HEAD.
* `macos/Flutter/GeneratedPluginRegistrant.swift` — auto-generated; CI
  reproduces it on build.

One change is **independently shippable**:
* `lib/features/workout/domain/services/workout_generator_service.dart`
  (Phase 100 home-only filter). Self-contained, no dangling imports.

Everything else is **partial-but-valuable** and needs companion files
recovered before it can be merged.

---

## 2 · Stash inventory

Each row lists the stash blob delta (pre → post hash), what HEAD
currently has, and the classification.

| # | Path | Stash pre | Stash post | HEAD | Class | Action |
|---|---|---|---|---|---|---|
| 1 | `RELEASE_BLACK_SCREEN_ROOT_CAUSE_REPORT.md` | 113135f | (deleted) | 113135f | D | discard or fold into archive PR |
| 2 | `RELEASE_FIXES_APPLIED.md` | 6e5d221 | (deleted) | 6e5d221 | D | discard or fold into archive PR |
| 3 | `RELEASE_HARDENING_CHECKLIST.md` | ad33d90 | (deleted) | ad33d90 | D | discard or fold into archive PR |
| 4 | `docs/AI_CONTEXT_REPORT.md` | b9a5e80 | 8d05557 | b9a5e80 | C | cherry-pick docs hunk |
| 5 | `docs/MASTER_LAUNCH_ROADMAP.md` | 1fd334f | 899e859 | 1fd334f | B | cherry-pick docs hunk; status snapshot has drifted |
| 6 | `docs/MONETIZATION_LAUNCH_GUIDE.md` | 7e056f2 | 65fab73 | 7e056f2 | B | cherry-pick — SKU renames `_quarterly→_3month`, `_yearly→_annual` |
| 7 | `docs/ROADMAP.md` | dedc0ea | d432698 | dedc0ea | C | cherry-pick docs hunk |
| 8 | `lib/core/services/share_service.dart` | 05a6da0 | ed91375 | 05a6da0 | B | depends on `progress/domain/year_in_review_summary.dart` (missing) |
| 9 | `lib/core/utils/app_haptics.dart` | 5b743f9 | 38a75d2 | 5b743f9 | A | self-contained helper additions |
| 10 | `lib/core/widgets/share_templates.dart` | f6fc1b6 | b2f5e7e | f6fc1b6 | B | depends on `year_in_review_summary.dart` |
| 11 | `lib/features/home/presentation/widgets/gelisim_tab.dart` | 3b3fde9 | 716d556 | 3b3fde9 | B | massive — depends on 9 missing files (see §4) |
| 12 | `lib/features/progress/presentation/calendar_screen.dart` | 4e7499e | 9b2abcd | 4e7499e | B | depends on `widgets/session_detail_sheet.dart` (missing) |
| 13 | `lib/features/progress/presentation/widgets/badge_unlock_dialog.dart` | cd82d76 | 283476b | cd82d76 | B | rewritten to delegate to `badge_celebration_screen.dart` (missing) |
| 14 | `lib/features/progress/presentation/widgets/weekly_retrospective_card.dart` | e20576b | 95d11e2 | e20576b | B | depends on `domain/volume_metrics.dart` + `providers/volume_metrics_provider.dart` (missing) |
| 15 | `lib/features/progress/providers/badge_unlocks_provider.dart` | 8597454 | ead6281 | 8597454 | B | adds `BadgeVisual` catalog; depends on `streak_calculator.dart`, `volume_metrics.dart`, `session_log_repository.dart` (last is in HEAD) |
| 16 | `lib/features/workout/domain/services/workout_generator_service.dart` | 574a146 | 7ba4baf | 574a146 | A | Phase 100 home-only filter, self-contained |
| 17 | `lib/features/workout/models/workout_plan_model.dart` | 1af6138 | **4aa5ed0** | **4aa5ed0** | D | already in HEAD — no-op |
| 18 | `logs.txt` | 2211d80 | efb6fb0 | 2211d80 | D | 24,155-line developer-log truncation; not source |
| 19 | `macos/Flutter/GeneratedPluginRegistrant.swift` | d90e5b7 | 8c3beed | d90e5b7 | D | auto-generated by Flutter; CI reproduces |
| 20-27 | `photos/*.webp` (8 files) | tracked | (deleted) | still tracked + on disk | C | overlap with Tier 2-A CDN migration. Worth re-evaluating individually |
| 28 | `pubspec.lock` | d36665f | 98c7ebb | d36665f | D | CI re-runs `flutter pub get`; harmless |
| 29 | `pubspec.yaml` | 9db1804 | a7541f4 | 9db1804 | C | version `0.1.0+6 → 0.1.0+8` |
| 30 | `web/public/privacy.html` | 0719f64 | b6ba742 | 0719f64 | A | wording: "Delete account" clarification + email instruction |

**Class legend:** A = shippable as-is · B = partial but valuable · C =
experimental / minor · D = obsolete or discardable.

---

## 3 · Purpose grouping

### Group I — Progress Redesign (Phases 1.D / 1.E / 2.E / 2.F / 5.C)

Consumer-side changes in 8 lib files. Themes pulled from the
inline `Phase X.Y · …` comments:

| Phase | Surface | Stash file(s) |
|---|---|---|
| 1.D | `BadgeVisual` catalog (icons + accent colors for hex tiles) | `badge_unlocks_provider.dart` |
| 1.E | Badge celebration as full-screen route (was a dialog) | `badge_unlock_dialog.dart` |
| 2.E | MET-based real kcal in the weekly retrospective (replaces synthetic `kcalPerCompletedDay`) | `weekly_retrospective_card.dart` |
| 2.F | Tap a completed calendar cell → session detail sheet | `calendar_screen.dart` |
| 5.C | Year-in-Review share (template + share service path) | `share_service.dart`, `share_templates.dart` |
| (umbrella) | Massive Gelişim-tab rebuild — hero strip, hex tiles, session detail, YIR trigger | `gelisim_tab.dart` (2,171-line delta) |

This group **does not compile** without the following companion files,
which are in neither HEAD nor the stash:

* `lib/core/widgets/spring_tap.dart`
* `lib/core/widgets/staggered_fade.dart`
* `lib/features/progress/data/coach_copy_corpus.dart`
* `lib/features/progress/domain/streak_calculator.dart`
* `lib/features/progress/domain/volume_metrics.dart`
* `lib/features/progress/domain/year_in_review_summary.dart`
* `lib/features/progress/providers/volume_metrics_provider.dart`
* `lib/features/progress/presentation/widgets/session_detail_sheet.dart`
* `lib/features/progress/presentation/widgets/badge_celebration_screen.dart`
* `lib/features/progress/presentation/year_in_review_screen.dart`

### Group II — Phase 100 home-only workout filter

`workout_generator_service.dart`. The generator now restricts the
exercise pool to bodyweight + single-dumbbell + dumbbell-pair only,
ignoring the `hasEquipment` flag entirely (kept in the signature for
cache-fingerprint compatibility). Self-contained.

### Group III — Already in HEAD

`workout_plan_model.dart` (Phase 98 `premiumExercises`) — landed in
`7a742da` (the CI closure commit). The stash delta is identical, so
applying it would be a no-op.

### Group IV — Repo hygiene / cleanup

* Three root `RELEASE_*.md` files deleted (NOT yet covered by
  `56a5912 chore(docs): archive 50 root reports`).
* Eight photo deletions (some overlap with Tier 2-A CDN migration, but
  every one of the 8 is still tracked + on disk in HEAD).
* `logs.txt` truncation (24,155 lines).
* `pubspec.lock` drift (CI regenerates).
* `macos/Flutter/GeneratedPluginRegistrant.swift` (auto-generated).

### Group V — Docs / metadata

* `docs/MASTER_LAUNCH_ROADMAP.md` — status field updates (Phase 92-93
  marked done in stash; HEAD's snapshot is older).
* `docs/MONETIZATION_LAUNCH_GUIDE.md` — IAP SKU rename
  `formai_pro_quarterly → formai_pro_3month`,
  `formai_pro_yearly → formai_pro_annual`.
* `docs/AI_CONTEXT_REPORT.md`, `docs/ROADMAP.md` — small edits.
* `pubspec.yaml` — build-number bump `0.1.0+6 → 0.1.0+8`.
* `web/public/privacy.html` — Delete Account wording.

---

## 4 · Overlap with current branch

| Workstream from session context | Overlap with stash? |
|---|---|
| Auth (popup/loop, paywall hydration, routing) | **None.** Stash touches no auth code. |
| ML / AI Tier S / A / B | **None.** Stash touches no ML/pose/analyzer code. |
| Onboarding redesign | **None.** Stash touches no onboarding step or chrome. |
| Dashboard / Gelişim tab | **Heavy.** `gelisim_tab.dart` is the largest single change in the stash. |
| Monetization | **Light.** Only the IAP SKU rename in `MONETIZATION_LAUNCH_GUIDE.md` and a status flip in `MASTER_LAUNCH_ROADMAP.md`. No paywall code changes. |
| Tier 2-A meal CDN migration | **Light overlap.** Photo deletions in the stash predate or overlap with Tier 2-A, but the targeted photos are still tracked in HEAD, suggesting Tier 2-A removed different photos. |
| Current HEAD (closure + test repair) | **One direct collision:** `workout_plan_model.dart` — already in HEAD with identical post-state. **One indirect alignment:** the closure files I committed (`level_titles.dart`, `level_curve.dart`, `xp_provider.dart`, etc.) are imported by `gelisim_tab.dart` in the stash. The stash was created in the same uncommitted-WIP world that produced the closure files. |

---

## 5 · Risk assessment

| Risk | Severity | Why |
|---|---|---|
| Applying the whole stash leaves the build red | **HIGH** | 11 dangling imports across `gelisim_tab.dart`, `share_service.dart`, `badge_unlock_dialog.dart`, `badge_unlocks_provider.dart`, `weekly_retrospective_card.dart`, `calendar_screen.dart`, `share_templates.dart`. |
| Mid-development snapshot vs. shippable state | **HIGH** | The stash represents the surface of a redesign where the consumer code was rewired but the underlying domain files (`volume_metrics`, `streak_calculator`, `year_in_review_summary`, `session_detail_sheet`, `badge_celebration_screen`, `spring_tap`, `staggered_fade`, `coach_copy_corpus`) were never staged. |
| Stale doc-status snapshots overwriting newer notes | **LOW-MEDIUM** | `MASTER_LAUNCH_ROADMAP.md` was a "Phase 92-93 done" view; HEAD may have since absorbed Phase 100+ status. Cherry-picking the docs needs a manual read first. |
| Photo deletions removing assets that are still imported | **MEDIUM** | All 8 targeted photos are still tracked. Need to grep `lib/` for each filename before deleting; deleting an in-use webp will surface as a runtime asset-load failure, not a compile error. |
| `pubspec.yaml` version bump landing twice | **LOW** | If the build number was already advanced past `0.1.0+8` between stash creation and now, applying the stash would silently downgrade. Worth checking before cherry-pick. |
| `Phase 100` home-only filter quietly removing gym variants | **MEDIUM** | This is a *behavioral* change: any user with `hasEquipment=true` previously could see barbell / cable / machine exercises, and won't anymore. Needs a PM sign-off before shipping, even though the diff itself is clean. |

---

## 6 · Per-file recommendations

| File | Recommended action | Rationale |
|---|---|---|
| `RELEASE_*.md` (×3) | **Discard from stash; archive separately** if desired. | Pattern matches the prior `56a5912` archive sweep — fold into a follow-up PR rather than mixing with code changes. |
| `docs/AI_CONTEXT_REPORT.md`, `docs/ROADMAP.md` | **Cherry-pick** as a docs-only PR. | Low-risk text edits. |
| `docs/MASTER_LAUNCH_ROADMAP.md` | **Cherry-pick after manual review** of current state. | Status fields may have drifted; don't overwrite blindly. |
| `docs/MONETIZATION_LAUNCH_GUIDE.md` | **Cherry-pick alongside a code-side check** that the new SKU names are what RevenueCat is actually configured with. | If the App Store / Play Console still has `_quarterly` / `_yearly`, applying this would leave docs lying. |
| `lib/core/utils/app_haptics.dart` | **Cherry-pick now.** | Only +7 lines, self-contained haptic helpers. |
| `lib/core/services/share_service.dart` | **Hold** until Group I companion files are recovered. | Adds the YIR share path; references `year_in_review_summary.dart`. |
| `lib/core/widgets/share_templates.dart` | **Hold** with `share_service.dart`. | Same YIR dependency chain. |
| `lib/features/home/presentation/widgets/gelisim_tab.dart` | **Hold** — needs all 9 Progress-Redesign companion files first. | Largest single change; central to the redesign. |
| `lib/features/progress/presentation/calendar_screen.dart` | **Hold** — needs `session_detail_sheet.dart`. | Tap-target wiring only; the cell rewrite itself is small. |
| `lib/features/progress/presentation/widgets/badge_unlock_dialog.dart` | **Hold** — needs `badge_celebration_screen.dart`. | The stash version is essentially "delete the inline dialog, delegate to a new screen." |
| `lib/features/progress/presentation/widgets/weekly_retrospective_card.dart` | **Hold** — needs `volume_metrics_provider.dart`. | Switch from synthetic kcal to MET-based real kcal. |
| `lib/features/progress/providers/badge_unlocks_provider.dart` | **Hold** — needs `streak_calculator.dart`, `volume_metrics.dart`. | Adds `BadgeVisual` catalog used by the hex tiles. |
| `lib/features/workout/domain/services/workout_generator_service.dart` | **Ship independently** as its own PR. | Phase 100 home-only filter is self-contained, no missing deps. PM sign-off recommended given the behavior change. |
| `lib/features/workout/models/workout_plan_model.dart` | **Drop** from stash; already in HEAD. | No-op. |
| `logs.txt` | **Drop** from stash; not source. | Dev artifact. |
| `macos/Flutter/GeneratedPluginRegistrant.swift` | **Drop** from stash; regenerated. | Auto-generated. |
| `photos/*.webp` (×8) | **Defer** — grep each filename in `lib/` first, evaluate per-photo against the Tier 2-A CDN inventory. | Some may genuinely be unused; some may still be referenced. Per-photo decision. |
| `pubspec.lock` | **Drop** from stash; CI regenerates. | Drift only. |
| `pubspec.yaml` | **Cherry-pick after checking** current build number isn't already past `+8`. | Minor metadata. |
| `web/public/privacy.html` | **Cherry-pick now.** | Clearer wording for users; no code impact. |

---

## 7 · Recovery strategy (suggested)

Track 1 (low-risk, ready now):
1. New PR — `chore: archive RELEASE_*.md root reports` (continue 56a5912).
2. New PR — `docs: refresh launch status + privacy wording + SKU names`.
3. New PR — `chore(app): add haptic helpers` (just the 7-line
   `app_haptics.dart` additions).

Track 2 (single isolated feature):
4. New PR — `feat(workout): Phase 100 home-only generator filter`.
   Standalone; needs PM/UX sign-off on the behavior change.

Track 3 (Progress Redesign — requires file recovery first):
5. Locate the missing companion files. Likely locations to check, in
   priority order:
   * earlier commits on the working machine (`git reflog` /
     `git fsck --lost-found` for dangling blobs);
   * sibling branches or remote PRs;
   * the developer's local working trees on other machines.
6. Once recovered, reconstitute as a coherent multi-PR series:
   * `feat(progress): volume metrics + streak calculator (domain)`
   * `feat(progress): badge visuals + celebration screen`
   * `feat(progress): session detail sheet + calendar tap`
   * `feat(progress): weekly retrospective MET-based kcal`
   * `feat(progress): year-in-review screen + share path`
   * `feat(progress): Gelişim-tab v2 (consumer-side rebuild)` — last,
     once all of the above are in.

Track 4 (cleanup, defer):
7. Per-photo audit against Tier 2-A and current asset usage. Drop the
   `logs.txt`, `pubspec.lock`, and `macos/...` portions of the stash —
   they aren't worth carrying.

---

## 8 · Final summary

* **Valuable?** Yes. The Progress Redesign represents Phases 1.D / 1.E /
  2.E / 2.F / 5.C of a coordinated dashboard rebuild. The Phase 100
  home-only filter is independent value.
* **Dangerous to apply?** Yes — 11 dangling imports would leave the
  build red. Do **not** `git stash pop`.
* **Obsolete?** Partly. `workout_plan_model.dart`, `logs.txt`,
  `macos/...`, `pubspec.lock`, and the 3 `RELEASE_*.md` deletions are
  either already done or hygiene artifacts.
* **What next?** Keep the stash where it is. Execute the four-track
  recovery in §7. Start with Tracks 1, 2, 4 (low-risk shippables and
  defer/drop calls), then unblock Track 3 by locating the missing
  Progress-Redesign companion files.
