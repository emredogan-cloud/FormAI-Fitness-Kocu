# Phase 2 (P-Risk) · gen-l10n configuration (F-i18n localization foundation).
# Output goes to a real file under lib/l10n (synthetic-package: false) so the
# import is explicit and version-stable.
arb-dir: lib/l10n
template-arb-file: app_en.arb
output-dir: lib/l10n
output-localization-file: app_localizations.dart
output-class: AppLocalizations
nullable-getter: false
